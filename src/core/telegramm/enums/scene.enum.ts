export enum SceneEnum {
  startScene = 'start',
  weatherScene = 'weather',
  timeScene = 'time',
  catScene = 'cat',
  dogScene = 'dog',
  helpScene = 'help',
  sightScene = 'sight',
  taskScene = 'task',
  dateScene = 'date',
}
